[workspace]
resolver = "3"
members = [
    "crates/libfw-core",
    "crates/libfw-server",
    "crates/libfw-client",
    "examples/axum-server",
    "examples/actix-server",
]

[workspace.package]
version = "0.1.4"
edition = "2024"
license = "MIT"
repository = "https://github.com/ArthurZhou/libfw"
description = "High-performance streaming file & folder transfer library for Rust"

[workspace.dependencies]
# NB: `version` is required so `cargo publish` can rewrite the path to a
# crates.io dependency when publishing libfw-server / libfw-client.
libfw-core = { path = "crates/libfw-core", version = "0.1.0" }
libfw-server = { path = "crates/libfw-server", version = "0.1.0" }
libfw-client = { path = "crates/libfw-client", version = "0.1.0" }

# Core / shared
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
zrip = { version = "0.8", features = ["frame"] }

# Server (axum integration example)
axum = { version = "0.8", features = ["http1", "tokio"] }
tokio = { version = "1", features = ["full"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["trace"] }
http = "1"
bytes = "1"
async-trait = "0.1"
futures = "0.3"
sha2 = "0.10"
time = "0.3"

# Client (WASM)
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
js-sys = "0.3"
web-sys = "0.3"
# Example servers
actix-web = "4"
actix-rt = "2"

getrandom = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
