[package]
name = "libfw-actix-server"
version = "0.1.0"
edition = "2024"
publish = false
description = "libfw integration example: an actix-web based file server"

[dependencies]
libfw-core = { path = "../../crates/libfw-core" }
libfw-server = { path = "../../crates/libfw-server", features = ["path-encrypt"] }
actix-web = "4"
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
futures = "0.3"
tracing = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"